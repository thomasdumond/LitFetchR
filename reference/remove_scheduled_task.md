# Remove a scheduled task by name from Task Scheduler (Windows) or Cron (Mac/Linux)

Remove a scheduled task by name from Task Scheduler (Windows) or Cron
(Mac/Linux)

## Usage

``` r
remove_scheduled_task(taskname)
```

## Arguments

- taskname:

  Name/ID of the scheduled task (Windows Task Scheduler or cronR id)
