# Create a unique name for any document

Create a unique name for any document

## Usage

``` r
build_sheet_name(time = Sys.time())
```

## Arguments

- time:

  System time at the time the function is run

## Value

Return a unique name based on the system time with the format
YYYY-MM-DD-HHMMSS.
